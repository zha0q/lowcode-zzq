import { positionField } from './PositionField';
import { unitInputField } from './UnitInputField';
import { urlInputField } from './UrlInputField';

export default {
  unitInput: unitInputField,
  urlInput: urlInputField,
  position: positionField,
  // 自定义主题名字，随意取
  theme: 'customTheme',
};
